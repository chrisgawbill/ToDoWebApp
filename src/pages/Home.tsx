import { useState} from "react";

function Home(){
    let [todo, setToDo]= useState([])
    return(
        <div>
            for(task in todo){
                
            }
        </div>
    );
}
export default Home;
